{
  "name": "tradingview-gainers-scanner",
  "version": "0.1.0",
  "private": true,
  "description": "Live TradingView top-gainers scanner. Drives a real headless Chromium against tradingview.com (JS-rendered, no static HTML) and runs a 4-step trend/size/10x pipeline.",
  "type": "module",
  "bin": {
    "tv-scan": "src/scan.js"
  },
  "scripts": {
    "scan": "node src/scan.js",
    "scan:json": "node src/scan.js --json"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "playwright": "^1.56.1"
  }
}
